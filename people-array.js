let people = {
  name: ("jurģis", "oskars", "gatis", "haralds"),
  age: (11, 15),
  isStudent: (false, true),
};
let i = people.name.length;
for (let i = 0; i < 3; i++) {
  console.log("Vārds: " + people.name[i]);
}
